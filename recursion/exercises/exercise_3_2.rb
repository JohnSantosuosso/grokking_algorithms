#You accidentally write a recursive function that runs forever
#What happens to the stack when your recursive functions runs forever?
#It will have infinite boxes added to it without a breaking condition

